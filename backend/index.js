const axios = require('axios');

console.log("Iniciando ponte com o Sienge...");

// Exemplo de como seria a chamada (a gente vai ajustar com seus dados reais)
async function testeConexao() {
    try {
        console.log("Tentando falar com a API...");
        // Aqui depois colocaremos a URL real do seu Sienge
    } catch (error) {
        console.error("Erro na conexão");
    }
}

testeConexao();